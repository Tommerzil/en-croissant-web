# copy to .env next to compose.yaml; set UID/GID to the owner of ./data and ./engines (`id -u`, `id -g`)
CHESS_TAG=latest
CHESS_UID=1000
CHESS_GID=1000
# hard CPU limit for the container; must not exceed the host's core count (`nproc`) or compose up fails
CHESS_CPUS=3
